import React from 'react';
import ReactDOM from 'react-dom';

function SampleButton() {
	return(
		<>
			<h1> hello World </h1>
			<button> Change to my name </button>
		</>
	);
}

export default SampleButton;